# Server (optional)

The game runs without this folder on GitHub Pages.

Use a server only when you later want:
- real generative AI
- multiplayer
- cloud saves
- shared economy/rankings

Never put AI provider API keys inside `index.html` or browser JavaScript.

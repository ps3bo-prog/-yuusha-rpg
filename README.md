# 勇者の冒険 REAL v7.1.1 3D

修正内容:
- 左スティックの左右入力を反転修正
  - 左へ倒す → 左へ移動
  - 右へ倒す → 右へ移動
- 画面ドラッグによるカメラ操作はそのまま維持
- 上下移動・攻撃・回避・スキル等も維持

GitHub Pagesでは既存の `index.html` をこのファイルに置き換えてCommitしてください。
確認時はURL末尾に `?v=711` を付けると古いキャッシュを避けやすいです。

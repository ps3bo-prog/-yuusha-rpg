// REAL v19 PlayerController scaffold
// Future home for movement, camera-relative input, animation state and equipment visuals.
export class PlayerController {
  constructor({ player, camera }) { this.player = player; this.camera = camera; }
  update() {}
}

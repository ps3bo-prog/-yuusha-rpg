// REAL v19 ChunkManager scaffold
// Future home for streamed open-world chunks and region loading.
export class ChunkManager {
  constructor({ chunkSize = 24 } = {}) { this.chunkSize = chunkSize; }
  update() {}
}
